# Database Configuration
database_host: localhost
database_password: example_db_password
api_key: sk_example_key
token: example.jwt.token
secret: example_app_secret
credential: AKIAEXAMPLE000000000
auth_key: auth_token_example
# This comment line is not cloaked
public_setting: visible_value

# Nested configuration
services:
  api:
    password: nested_secret_example
    apiKey: nested_api_key_example
